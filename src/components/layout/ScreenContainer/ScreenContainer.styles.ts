import { StyleSheet } from 'react-native';

import {
  COLORS,
  SPACING,
} from '@/theme';

export const styles = StyleSheet.create({
  container: {
    flex: 1,

    backgroundColor: COLORS.bg,

    padding: SPACING.lg,
  },
});